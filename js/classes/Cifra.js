/**
 * Representação de linhas cifradas
 */
class Cifra {
    constructor(linha) {
      // Linha onde fica a tablatura, permite que seja ordenada juntamento com as cifras
      this.linha = linha || 0
    }
}